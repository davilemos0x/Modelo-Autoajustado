/*select 
*,
EXTRACT(MONTH FROM data) as "Mes",
EXTRACT(YEAR FROM data) as "Ano"
from pastagens order by data asc

select 
*
from pastagens_ta order by ano, mes asc
*/
select 
*
from pastagens_ca order by ano, mes asc
